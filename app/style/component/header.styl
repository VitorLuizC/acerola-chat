.header
  border-bottom: 1px solid $header-color

  > .title
    font-size: 3em
    font-weight: 100
    color: $header-color
    text-align: center
    text-shadow: 0 0 5px rgba(#000, .75)

    +above(540px)
      font-size: 4em
      text-shadow: 0 0 10px rgba(#000, .75)
